import { QCalibration } from '@/features/onboarding/screens/QCalibration';

/** Thin route (01 §2) — the feature owns the logic. */
export default function QCalibrationRoute() {
  return <QCalibration />;
}
