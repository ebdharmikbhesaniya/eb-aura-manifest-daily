import { S12NotificationsMore } from '@/features/onboarding/screens/S12NotificationsMore';

/** Thin route (01 §2) — the feature owns the logic. */
export default function S12bNotificationsRoute() {
  return <S12NotificationsMore />;
}
