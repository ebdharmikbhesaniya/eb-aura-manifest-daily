import { A12Reminder } from '@/features/onboarding/screens/A12Reminder';

/** Thin route (01 §2) — the feature owns the logic. */
export default function A12ReminderRoute() {
  return <A12Reminder />;
}
