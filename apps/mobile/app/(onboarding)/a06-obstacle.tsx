import { A06Obstacle } from '@/features/onboarding/screens/A06Obstacle';

/** Thin route (01 §2) — the feature owns the logic. */
export default function A06ObstacleRoute() {
  return <A06Obstacle />;
}
