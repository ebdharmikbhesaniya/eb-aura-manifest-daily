import { A10Commitment } from '@/features/onboarding/screens/A10Commitment';

/** Thin route (01 §2) — the feature owns the logic. */
export default function A10CommitmentRoute() {
  return <A10Commitment />;
}
