import { A03SocialProof } from '@/features/onboarding/screens/A03SocialProof';

/** Thin route (01 §2) — the feature owns the logic. */
export default function A03SocialProofRoute() {
  return <A03SocialProof />;
}
