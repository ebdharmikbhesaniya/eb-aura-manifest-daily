import { A08RitualTime } from '@/features/onboarding/screens/A08RitualTime';

/** Thin route (01 §2) — the feature owns the logic. */
export default function A08RitualTimeRoute() {
  return <A08RitualTime />;
}
