import { S12Notifications } from '@/features/onboarding/screens/S12Notifications';

/** Thin route (01 §2) — the feature owns the logic. */
export default function S12NotificationsRoute() {
  return <S12Notifications />;
}
